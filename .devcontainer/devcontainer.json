{
  "name": "Haskell + Python Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",

  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.12"
    }
  },

  "postCreateCommand": "curl --proto '=https' --tlsv1.2 -sSf https://get-ghcup.haskell.org | BOOTSTRAP_HASKELL_NONINTERACTIVE=1 BOOTSTRAP_HASKELL_INSTALL_STACK=1 sh && echo 'export PATH=$HOME/.ghcup/bin:$HOME/.local/bin:$PATH' >> ~/.bashrc",

  "customizations": {
    "vscode": {
      "extensions": [
        "haskell.haskell",
        "justusadam.language-haskell",
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },

  "hostRequirements": {
    "cpus": 2,
    "memory": "4gb"
  }
}
